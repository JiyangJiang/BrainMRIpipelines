#!/bin/bash

cohortFolder=$1
cleanup_mode=$2

# generate temporal SD map for visual QC
curr_dir=$(dirname $(which $0))

${curr_dir}/L-lv_qc_mniSpace_Tstd.sh ${cohortFolder} \
									 ${cleanup_mode} \
									 yesQsub